[About]
Version=1.0
Author=Dilma

[Type]
Class=Element
Info=Запоминает данные из потока
Tpl=True

[Property]
+Default=Хранит данные по умолчанию|3|

[Methods]
doValue=Запоминает данные из потока|1|
doClear=Устанавливает ячейку памяти в исходное состояние (т.е. приравнивает её значение значению по-умолчанию)|1|
onData=Событие происходит всякий раз после изменения данных, их же и выдавая обратно в поток|2|data
Value=Содержит данные из ячейки памяти|3|data
