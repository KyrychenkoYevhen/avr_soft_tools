func doGet(_data):java
  event(onGet, Array && '[' && Name && ']')
end

func doSet(_data):java
  println(Array, '[', Name, '] = ', _data_, ';')
  event(onGet, Array && '[' && Name && ']')
end

func Get():java
  return(Array && '[' && Name && ']')
end

